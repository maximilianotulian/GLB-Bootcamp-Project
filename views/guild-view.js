var React = require('react');

var GuildView = React.createClass({
    render: function () {
        return (
            <div>
                Guild
            </div>
        )
    }
});

module.exports = GuildView;
